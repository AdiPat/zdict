#include "zdict_internal.h"

/* Mutable mode implementation */
int zdict_mutable_init(zdict_internal_t *zdict) {
    /* Mutable mode uses standard dict operations */
    /* No special initialization needed */
    return 0;
}